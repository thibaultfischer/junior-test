/* import {} from '../actions/app.actions'; */

const initialState = {
  showImages: true,
};

const appReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default appReducer;
